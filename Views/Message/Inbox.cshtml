@model List<MittClick.Models.Message>

@{
    ViewData["Title"] = "Inbox";
}

<div class="container mt-4">
    <h2>Inbox</h2>

    @if (Model.Count > 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <th>Från</th>
                    <th>Meddelande</th>
                    <th>Status</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var message in Model)
                {
                    <tr>
                        <td>@message.SenderName</td>
                        <td>@message.Text</td>
                        <td>@(message.IsRead ? "Läst" : "Oläst")</td>
                        <td>
                            @if (message.IsRead == false)
                            {
                              <a class="btn btn-light" asp-action="MarkAsRead" asp-controller="Message" asp-route-messageId="@message.MessageId">Markera som läst</a>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>Din inkorg är tom.</p>
    }
</div>
